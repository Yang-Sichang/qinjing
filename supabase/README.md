# 琴境 Supabase

The frontend uses the public Supabase project URL and publishable/anon key from `client-config.js`.

Before the shared 语印 system can work, run `schema.sql` in the Supabase SQL Editor and enable Anonymous Sign-Ins under Authentication.

Do not put a Supabase `service_role` key in the repository or browser code.
